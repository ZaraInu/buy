import "./globals.css";

export const metadata = {
  title: "Buy To Hold (Buy To Hold) | Solana",
  description: "Dui is forever on Solana",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="">{children}</body>
    </html>
  );
}
